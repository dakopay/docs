## Token Details

```jsx
import { tokenDetails } from 'repa-sdk';

var td = await tokenDetails('0x00000000');
```

## User Token Details

```jsx
import { getUserTokenData } from 'repa-sdk';

var td = await getUserTokenData(_token, _user);
```
