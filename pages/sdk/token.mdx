## Token Details

```jsx
import { tokenDetails } from 'dakopay-sdk';

var td = await tokenDetails('0x00000000');
```

## User Token Details

```jsx
import { getUserTokenData } from 'dakopay-sdk';

var td = await getUserTokenData(_token, _user);
```
