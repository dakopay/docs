## graph Transfers

```jsx
import { graphTransfers } from 'dakopay-sdk';

var gt = await graphTransfers(`first : 20`);
```

## graph Subscriptions

```jsx
import { graphSubscriptions } from 'dakopay-sdk';

var gs = await graphSubscriptions(`first : 20`);
```
