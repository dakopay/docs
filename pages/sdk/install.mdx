Install the SDK in order to use our protocol:

## Install SDK

```jsx

// instal sdk
npm i dakopay/SDK

```

## Init SDK

```jsx
// Use
import { setChain } from 'dakopay-sdk';

// Set Network Chain
setChain(5);
```
