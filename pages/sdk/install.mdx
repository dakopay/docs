Install the SDK in order to use our protocol:

## Install SDK

```jsx

// instal sdk
npm i projectrepa/SDK

```

## Init SDK

```jsx
// Use
import { setChain } from 'repa-sdk';

// Set Network Chain
setChain(5);
```
